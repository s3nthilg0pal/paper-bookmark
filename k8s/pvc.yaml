apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: paper-bookmark-data
  namespace: paper-bookmark
  labels:
    app.kubernetes.io/name: paper-bookmark
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Uncomment and set your storage class if needed
  # storageClassName: standard
